server:
  servlet:
    context-path: /api
spring:
  liquibase:
    change-log: classpath*:migrations/master-changelog.yaml

  datasource:
    url: jdbc:postgresql://localhost:5432/delivery
    username: postgres
    password:  1111

